// Filename: my-fetch.ts
/*import { getAccessToken, verifyAccessToken } from "./auth.ts"

export const myFetch = async (
  url: RequestInfo | string,
  init: RequestInit = {},
): Promise<Response> => {
  // add the Authorizatoin header,
  // keep the rest
  init.headers = {
    ...init.headers,
    Authorization: `Bearer ${getAccessToken().accessToken}`,
  }

  return fetch(url, init)
}
 */

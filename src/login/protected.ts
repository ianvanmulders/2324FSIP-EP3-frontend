// Filename: protected.ts
/*
import { hasRole } from "../ts/utils/auth.ts"

if (!hasRole("member")) {
  location.assign(`../401/?from=${window.location.href}`)
  // I've added the 'from='
  // just in case I want to perform a redirect later,
  // after succesful login for instance.
}


 */
